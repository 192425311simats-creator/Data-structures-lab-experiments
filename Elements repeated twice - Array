#include <stdio.h>

int main() {
    int n, i, j;
    scanf("%d", &n);
    int arr[n], visited[n];
    for(i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
        visited[i] = 0;
    }
    for(i = 0; i < n; i++) {
        if(visited[i] == 0) {
            int count = 1;
            for(j = i + 1; j < n; j++) {
                if(arr[i] == arr[j]) {
                    count++;
                    visited[j] = 1;
                }
            }
            if(count == 2)
                printf("%d ", arr[i]);
        }
    }
    return 0;
}
